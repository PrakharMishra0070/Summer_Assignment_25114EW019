#include <stdio.h>

int main() {
    int num1, num2, highest;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    highest = (num1 > num2) ? num1 : num2;

    while (1) {
        if (highest % num1 == 0 && highest % num2 == 0) {
            printf("LCM = %d", highest);
            break;
        }
        max++;
    }

    return 0;
}